import 'package:flutter/material.dart';

class RedactarPage extends StatelessWidget {
  const RedactarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      //hacer los textfield parea escribir historia
      body: Center(child: Text('Ecribir historias'),),
    );
  }
}